class RoverController {
    fun move(input: String, rover: Rover): Rover {
        //var returnRover = Rover(1,1,FACING.NORTH)
        //input.forEach { returnRover = rover.receiveInput(it) }
        return rover
    }
}